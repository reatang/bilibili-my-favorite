USER_SESSDATA=cookie中的SESSDATA
USER_BILI_JCT=cookie中的bili_jct
USER_BUVID3=cookie中的buvid3
USER_AC_TIME_VALUE=window.localStorage.ac_time_value（可不填）
USESR_DEDE_USER_ID=cookie中的DedeUserID

# WEB_HOST="0.0.0.0" # 如果需要监听所有网络接口
# WEB_PORT=8080 # 如果需要修改端口
# DEBUG=True # 如果需要开启调试模式

# DOWNLOAD_TIMEOUT=15
# MAX_PAGES_PER_COLLECTION=100
# REQUEST_DELAY=1.0

# LOG_LEVEL="DEBUG"
# 如果不提供 LOG_FILE，会使用默认的 BASE_DIR / "logs" / "app.log"
# LOG_FILE="custom_logs/my_app.log"

# 如果不提供 DATABASE_NAME，会使用默认的 "bilibili_favorites.db"
# DATABASE_NAME="my_custom_favorites.db"

# 对于 COVERS_DIR 和 TEMPLATES_DIR，如果不提供，
# 它们会基于 BASE_DIR 和它们在类中定义的相对字符串（"covers", "templates"）来构建。
# 如果你想覆盖它们，可以这样做：
# COVERS_DIR="my_custom_covers_folder"
# TEMPLATES_DIR="my_custom_templates_folder"